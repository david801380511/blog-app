// Re-export the new db/categories.js API for compatibility
export { all as repoList, getById as repoGet, create as repoCreate, update as repoUpdate, remove as repoDelete } from '../db/categories.js';
